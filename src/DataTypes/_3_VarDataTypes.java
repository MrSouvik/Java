package DataTypes;

public class _3_VarDataTypes {
	public static void main(String[] args) {
		var number = 11;
		var name = "John";

		// var count; //var can't be used without assigning the value
		// number = "john"; // var data type value can't be changed
		// var value = null; // var data type can't be null
		// List<var> password = new ArrayList<>(); // var data type can't be used in generic
		// var sum = (a,b)->a+b; // var data types can't be used in lambda expression

		// var display(var number) { // var data types can't be used in method parameter & return type
		// return number;
		// }

		System.out.println(number);
	}
}
